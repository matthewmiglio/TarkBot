import type { NextConfig } from "next";

// Nothing to configure yet. The demo iframe is the only third-party thing on the
// page and it needs no config: it is a plain element, not next/image or a rewrite.
const nextConfig: NextConfig = {};

export default nextConfig;
